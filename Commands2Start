Type these in by hand:

sudo yum update
sudo yum install git
git config --global user.name "Jason Butcher"
git config --global user.email "jason@butcherville.com"
git clone https://github.com/jasontbutcher/bitstarter

./bitstarter ADStart

Then exit and run
./bitstarter ADRubyGo
